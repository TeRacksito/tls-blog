# Blocks

This directory contains high level, reusable components that are composed of ui components and/or other blocks. Usually, these components are more complex and have more logic than the ui components, but they are still meant to be reusable across different domains.

If a component is not reusable across different domains, it should be placed in the `featured` directory instead.
