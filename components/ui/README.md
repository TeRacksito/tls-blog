# ui

This folder contains generic, primitive components that implement specific UI elements with HTML and styling. These components are meant to be reusable across different domains, and should not contain any domain-specific logic or styling.

Each component could have variants and configurations.
